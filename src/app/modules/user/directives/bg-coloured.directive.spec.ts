import { BgColouredDirective } from './bg-coloured.directive';

describe('BgColouredDirective', () => {
  it('should create an instance', () => {
    const directive = new BgColouredDirective();
    expect(directive).toBeTruthy();
  });
});
