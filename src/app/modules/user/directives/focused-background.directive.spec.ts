import { FocusedBackgroundDirective } from './focused-background.directive';

describe('FocusedBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusedBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
